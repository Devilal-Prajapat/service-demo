# service-demo
Create a service in Linux

# Enable a service 
systemctl enable <your-service-name>

# Start a service 
systemctl start <your-service-name>

# stop a service 
systemctl start <your-service-name>
